---
import '../../index.css';
import BaseRefund from '../refund.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'id' } },
    { params: { lang: 'es' } },
    { params: { lang: 'pt-BR' } },
  ];
}

const { lang } = Astro.params;
---
<BaseRefund lang={lang} />