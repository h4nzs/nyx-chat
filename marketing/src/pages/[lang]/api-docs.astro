---
import '../../index.css';
import BaseApiDocs from '../api-docs.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'id' } },
    { params: { lang: 'es' } },
    { params: { lang: 'pt-BR' } },
  ];
}

const { lang } = Astro.params;
---
<BaseApiDocs lang={lang} />
