---
import '../../index.css';
import BaseIndex from '../index.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'id' } },
    { params: { lang: 'es' } },
    { params: { lang: 'pt-BR' } },
  ];
}

const { lang } = Astro.params;
---
<BaseIndex lang={lang} />
